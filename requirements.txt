# Chatterbox TTS Audiobook Edition - Requirements
# This file contains all dependencies needed to install Chatterbox TTS manually
# Alternative to using install-audiobook.bat

# Core PyTorch dependencies
# For GPU builds, install the matching +cuXXX wheels from https://download.pytorch.org/whl/cu124
torch==2.6.0
torchaudio==2.6.0

# Core Chatterbox TTS dependencies (from pyproject.toml)
numpy>=1.24.0,<2.0.0
ml-dtypes>=0.5.0
librosa==0.11.0
s3tokenizer
transformers==4.46.3
diffusers==0.29.0
resemble-perth==1.0.1
conformer==0.3.2
safetensors==0.5.3
spacy-pkuseg
pykakasi==2.3.0
gradio==5.44.1

# Audiobook app conveniences
scipy
soundfile
tqdm

# Optional dependencies for advanced NLP
# Uncomment the line below if you need enhanced language processing
# spacy[en_core_web_sm]>=3.4.0

# Installation Notes:
# 1. Make sure you have Python 3.10+ installed
# 2. For CUDA builds, swap the torch/torchaudio lines above to the matching +cuXXX wheels from PyTorch's download page
# 3. Install with: pip install -r requirements.txt
# 4. After installation, install the package with: pip install -e .
